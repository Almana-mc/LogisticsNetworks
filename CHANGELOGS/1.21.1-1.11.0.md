- Made fixes to node position
- Fixed a crash with regex filtering and tooltip checking on dedicated servers
- **All regex filters on tooltip mode will now revert to name mode.**
- Fixed overflow in regex filter textfield
- Filters are now virtualised, no actual items as filters, old filters will be deleted and transition to virtual filters will be automatic
- Fixed zh_cn translation errors(thanks Jerry)
- Performance changes, culled some faces of computer and nodes for improvements(thanks andrew)
- Fixed bug with computer not able to interact with distant nodes
