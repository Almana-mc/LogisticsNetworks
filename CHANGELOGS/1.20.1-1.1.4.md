- Added filter detail page (Ctrl+Left Click filter slot) with per-entry item ID, amount, NBT, durability, and slot mapping editing
- Added NBT table picker with operator support (=, !=, >=, <=) and raw SNBT mode with auto-sync
- Added nbtOnly filters: remove item from entry to match any item with configured NBT/durability/enchanted
- Added per-entry slot mapping to restrict filters to specific inventory slots (e.g. 0-8,13)
- Added JEI ghost ingredient support for the filter detail page
- Fixed setting a tag on a filter entry destroying existing NBT, durability, slot, and enchanted config
- Deprecated standalone Durability Filter and Slot Filter items (functionality moved to filter detail page)
- Removed standalone recipes for durability_filter and slot_filter
- Improved GUI to fit all settings without scrolling.
- Changed Import/Export to Reciever/Sender  respectively.
- Added naming for channels
- Cleaned up deprecations