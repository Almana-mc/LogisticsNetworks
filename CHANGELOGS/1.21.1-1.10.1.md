- Major lag improvements
- Removed cursor from round robin mode
- reduced hitbox of nodes to avoid interactions
- Fixed fluid filter entries ignoring their NBT rules when the entry also had a tag
- Fixed per-entry Stock and Batch applying to items that failed the entry's NBT/durability/enchanted rules
- Regex filter now matches item names only; tooltip matching removed as it crashed dedicated servers (#83)
