MOAR FIXES

- Fixed slot number overlay
- Added different channel tints depending on resource type being transferred
- Cleaned up tag screen, removed an icon from it
- Updated mek and ars upgrade textures
- Fixed several bugs with batch size not updating from certain conditions(resource type cycling and network fulfillment)
- Removed redundant redstone modes 'Always On/Off', old nodes will cycle to Low Signal by default and will get disabled if they were on 'Always off'
- Fixed a bug where items were not pulled from interfaces when network polling was added, now batch fulfillment will check for interfaces if network is not able to provide entire batch
- Added better messages for copy-paste failures
- Added deletion of networks, requires all nodes to be loaded to work, asks for confirmation
- Fixed a bug with filter persistence
- Fixed the copy paste UI to follow UI themes and the new virtual filter system
- Several smaller bug fixes
- Refactored nbt to not have fake entries, instead work with minecraft's native nbt tags
- Categorised NBT options to make it cleaner
- Added admin mode as a command to cleanup playing as an operator
- Admin mode command is `/logisticsnetwork admin [player]` player is an optional argument
- Removed save button from computer screen that was inherited with no function
- Reduced log spam of mod
- Changing resource type now clears the filter
