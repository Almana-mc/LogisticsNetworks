**26.1.2-1.12.1 - Fixes and quality of life**

- Removed the Pattern Setter from crafting and the creative tab; existing items remain compatible.
- Fixed GUI scale bug with nodes
- Added sender resource rotation for items and fluids.
- Fixed bug with functional storage interactions
- Added tooltip on opening advanced filtering
- Added confirmation(toggleable off) for creating networks while searching
- Added a circular mode selector to the mod, default keybind 'R'
- Ctrl + click now selects multiple nodes in the graph editor
- Shift drag now allows multi select in graph editor
- Multiple selected nodes can be added to a label in the graph editor
- Node table now has option to multi select and apply labels to nodes
- Added a new tag selection and viewer screen
- Nodes in graph editor are now hideable
- Retained this branch's existing upgrade-tier defaults and custom configuration values.
- Turned off async planning by default
